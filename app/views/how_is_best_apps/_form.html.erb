<%= form_for(@how_is_best_app) do |f| %>
  <% if @how_is_best_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how_is_best_app.errors.count, "error") %> prohibited this how_is_best_app from being saved:</h2>

      <ul>
      <% @how_is_best_app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
