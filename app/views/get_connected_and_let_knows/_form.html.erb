<%= form_for(@get_connected_and_let_know) do |f| %>
  <% if @get_connected_and_let_know.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@get_connected_and_let_know.errors.count, "error") %> prohibited this get_connected_and_let_know from being saved:</h2>

      <ul>
      <% @get_connected_and_let_know.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
