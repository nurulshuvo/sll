<%= form_for(@wap_based) do |f| %>
  <% if @wap_based.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wap_based.errors.count, "error") %> prohibited this wap_based from being saved:</h2>

      <ul>
      <% @wap_based.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
